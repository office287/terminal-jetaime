.DS_Store
node_modules/
package-lock.json

# Local env files — never commit secrets
.env
.env.local
.env.*.local

# Runtime artifact from nightly-improve.js (read by morning-report.js)
reports/.last-nightly.json

# Visit store written by server.js (served via /__stats)
data/hits.jsonl
